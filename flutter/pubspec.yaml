name: flutter_library
description: Flutter libraries
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  get_storage: ^2.1.1
  http: ^1.1.0
  http_parser: ^4.0.2
  image_picker: ^1.0.7
  loading_animation_widget: ^1.2.0+4
  permission_handler: ^11.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/forest.jpg
